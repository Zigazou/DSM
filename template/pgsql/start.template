#!/bin/bash
nohup "$DAEMON" -D "$DATADIR" > "$LOGPATH" 2>&1 &
for loop in {1..30}; do [ -f "$PIDPATH" ] && break; sleep 0.5; done
notify-send --expire-time=1500 --icon=list-add "$SITE - PostGreSQL started"

